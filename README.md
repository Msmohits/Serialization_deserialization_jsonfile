# Serialization_deserialization_jsonfile
Clear concept of serialization and deserialization . serializing data into check.json file and after that get that data back by deserializing the check.json file.  
